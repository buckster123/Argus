[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    hardware: tests that require physical hardware (webcam, mic)
    display: tests that require a display server
